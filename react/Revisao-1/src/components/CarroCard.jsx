import React from 'react';

const CarroCard = ({}) => {

  return (
    <div className="carro-card">
      <img src={} alt={} />
      <h3>{}</h3>
      <p>{}</p>
      <p>Ano: {}</p>
      <button onClick={}>Mais Detalhes</button>
    </div>
  );
};

export default CarroCard;
